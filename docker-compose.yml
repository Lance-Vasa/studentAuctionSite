version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: student_market
      POSTGRES_PASSWORD: secure_password
      POSTGRES_DB: student_market_db
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
