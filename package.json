{
  "name": "student-auction-site",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "start:all": "npm install && npm run install:all && npm run dev",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "docker compose up -d && concurrently -k -n backend,frontend -c cyan,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "stop": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
